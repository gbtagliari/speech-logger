# Working directory: wayfinder maps, tickets, voice samples, model bake-off outputs.
# Contains personal voice recordings and braindump transcripts. Never publish.
.scratch/

# Local sandbox tooling config, personal to the machine, not part of the project.
.ai-jail
.probe/

# macOS Finder metadata.
.DS_Store

# Tuist / Xcode generated artifacts. Project.swift is the source of truth;
# the project and workspace are regenerated with `tuist generate`.
*.xcodeproj
*.xcworkspace
/Derived/
.build/
Tuist/.build/
*.xcuserdatad
xcuserdata/
